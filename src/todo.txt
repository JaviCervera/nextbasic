V0.5:
+ Improve options of Extern blocks
+ Fix bug width negative literals
+ Create Geany definition file
* Add SDL, SDL_mixer, SDL_image, SDL_net, SDL_gfx modules (SDL2)
- Update information on webpage
- Replace forums with phpBB
- Add PayPal donation link

V0.5.1:
- Struct field names clash with global names
- Add Opaque structs
- Add bitwise operators (&, |, ^, Shl, Shr)
- Change arrays to std::vector (no CreateArray nor FreeArray, allocate on stack with Dim arr(len, len...) or Dim arr(,...)
- Pass strings and arrays by reference (return copy)
- Create TextWrangler definition file


V0.6:
- Create Lua module

- Add Irrlicht module
- Add INI module
- Add JSON module
- Add XML module

V0.7:
- Add Android and iOS targets

V0.8:
- Create Gui module based on LCL

V1.0:
- Add doc generator
- Add documentation
- Add examples
- Add unit tests

V1.0+:
- Visual GUI editor
- Fix bundled app path in Snow Leopard
- Add "." as operator in parser (do not use the ParseVarAccess crap anymore)
- Copy shared libraries with executable
- Add StaticLib, SharedLib to $Option directive (static and shared lib generation support)
- Add $If, $ElseIf, $Else, $EndIf, with directives: Windows, Mac, Linux, iOS, Android, Phone, Tablet
- Rewrite compiler in NextBasic?
- Add unicode support
- Add multithreading
- Add debugger
